import 'package:flutter/material.dart';

class TaskDetailsListeners extends StatelessWidget {
  const TaskDetailsListeners({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
